// Re-export della pagina docente (fonte unica). Montata dentro il cockpit:
// legge params.sectionId (dal segmento [sectionId]) e ?userId=.
export { default } from '@/app/(dashboard)/teacher/primaria/[sectionId]/registro/page';
