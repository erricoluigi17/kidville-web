<?xml version="1.0" encoding="utf-8"?>
<!--
  Configurazione di sicurezza di rete — variante DEBUG.

  Sovrascrive `src/main/res/xml/network_security_config.xml` solo nel buildType debug,
  e riapre il traffico in chiaro verso gli host locali usati dal live-reload:
  10.0.2.2 (loopback dell'host visto dall'emulatore), localhost e 127.0.0.1.

  Sta in `src/debug/` di proposito: nel bundle di release questi indirizzi non compaiono
  affatto, così la dichiarazione «tutti i dati utente sono cifrati in transito» del modulo
  Sicurezza dei dati di Google Play regge anche a un'analisi statica dell'AAB.

  Il `domain-config` esplicito (e non le sole `debug-overrides`) serve perché AOSP può non
  onorare `cleartextTrafficPermitted` dentro `debug-overrides`.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
