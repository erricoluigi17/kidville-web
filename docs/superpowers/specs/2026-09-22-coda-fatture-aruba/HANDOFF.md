# HANDOFF — Coda fatture Aruba (riprendere da qui)

> Documento per la sessione che riprende il lavoro. Stato aggiornato al **24/09/2026 03:20**.
> Leggere nell'ordine: questo file → `nucleo.md` → `contratto.md` (solo per la seconda consegna).

## 1. Cosa è in produzione

| Rilascio | PR | Merge | Cosa fa |
|---|---|---|---|
| **R1 — correzione urgente** | #159 | 23/09 12:07 (`11f59af1`), deploy 12:09 | Tolto il vincolo `UNIQUE (scuola_id, anno, numero)` che rifiutava dal registro fatture già partite. 409 `FATTURA_PARTITA_NON_REGISTRATA`. Tetto 50 sul salto del pavimento, con log del pavimento. 23505 distinto per vincolo. Script `scripts/numerazione-serie.mjs` (indagine) e `scripts/fatture-orfane.mjs` (registrazione delle orfane) |
| **Nucleo della coda** | #160 | 23/09 15:34 (`a447e4ce`), deploy 15:36:54, primo battito del cron alle 15:37:02, 8 secondi dopo il deploy (`niente-da-fare`, letto in `app_log`) | Coda persistente (`fatture_coda`, `fatture_coda_stato`); cron `fatture-coda-tick` ogni 5 minuti fuori dalle finestre della sync; lavoratore unico; 50/ora fail-closed; pausa 60 minuti su 429 e 15 minuti su esito incerto. Pagina «Coda fatture». Il lotto in riconciliazione accoda (fino a 500); il pulsante singolo accoda in testa |
| **PR-B** | #161 | 23/09 17:44 (`f59ef7ea`) | Cinque fotografie rigenerate dalla produzione dopo l'applicazione della migrazione del nucleo (migrazioni, policy, indici unici, FK, tabelle con `scuola_id`) e `MIGRAZIONI_ATTESE_AL_MERGE` svuotata; `fatture-coda-tick` in `JOB_CRON` con finestra di 30 minuti; lock `isolamento-sede` da 109 a 113 esenzioni, con quattro voci nuove in `AMMESSE` (`coda:GET`, `coda:<modulo>`, `coda/azioni:POST`, `video-uploads/[id]:<modulo>`); guardia degli script (`scripts/lib/aruba-lettura.mjs`) col ramo del nucleo: `fatture-orfane` e `numerazione-serie` partono solo a coda sospesa, fuori dalla pausa del lavoratore (429/esito incerto) e senza lavoratore attivo; questo HANDOFF |
| **Consegna 2a** | #162 | 23/09 23:06 (`c5fd3db7`), deploy 23:07:32 | Rilievi (b)–(e) di §3: «Togli» azzera anche l'esito; pausa e fine stimata col giorno; via il codice morto del vecchio lotto e le 18 chiavi `reconLotto*`; chip della coda sulle righe di Pagamenti e Riconciliazione. La migrazione `20260923191725_fatture_coda_togli_azzera_esito.sql` l'ha applicata l'integrazione al merge, e l'hanno verificata sole letture (24/09): `md5` del corpo di `fatture_coda_togli` in produzione `c97265ed07297d841ba8496334770641`, uguale al file; definer, `search_path=public, pg_temp`, EXECUTE al solo `service_role`; nessun nome doppio in `schema_migrations`. Il run «DB migrate (prod)» `35920312716` resta in attesa: **non si approva**. Piano: `consegna-2a-rilievi.md` |
| **Consegna 2b** | — | — | Branch `fix/coda-fatture-rifiniture-2b`. D1 «Altro» in coda, validato all'accodamento, e «ricorda sulla scheda» scritto dal lavoratore; D2 «Togli», «Rimetti» e «Sospendi/Riprendi» loggano l'attore; D3 la stima conta le emesse dell'ultima ora; D5 le righe con una voce attiva escono da «Da fatturare», dal lotto e da «Invia fattura»; D6 il chip della coda nel popup e nel drawer; D7 «Errore in coda» è un collegamento alla «Coda fatture»; D9 via `corpoEmissione`; D10 i testi che dicevano «emetti» mentre si accoda; D11 «Oggi»/«Ieri» della chat in Europe/Rome; D12 il cruscotto si aggiorna dopo un accodamento; D13 `chiudi('emessa')` azzera il messaggio; D14 `gallery.published` consegnato con la ricevuta della retention. Migrazione `20260924010455_fatture_coda_chiudi_emessa_azzera_messaggio.sql`. Piano: `consegna-2b-rifiniture.md`. ⏳ **Da fare dopo il deploy `READY`, dalla sessione principale e mostrandola prima**: la ripresa dei `gallery.published` in quarantena (13 al 24/09) con l'`UPDATE` di §6.5 del piano (`attempts = 0`, lease azzerata: nessuna cancellazione), poi il controllo di §6.5c. Lo stato di produzione (PR, merge, deploy, migrazione verificata, scrittura D14 con data e numero) lo registra il primo commit del branch che segue il deploy (piano §6.6, punto 6) |

**Misurato al 24/09 alle 03:18** (solo `SELECT` in produzione): `fatture_coda` ha **0 voci**, e l'ultima migrazione applicata è `20260923191725` (la 2a). Il cardine (decisione 0: una fattura accodata parte dal lavoratore anche a PC spento) non l'ha ancora provato nessuna fattura vera: la prova dal vivo resta da fare con le prime fatture della segreteria (e con quelle anche il chip sulle righe e la stima col giorno).

**Come si usa oggi (segreteria)**
1. Contabilità → Pagamenti → Riconciliazione: seleziona le fatture, fai il pre-controllo, conferma. Le fatture **vanno in coda** e si può chiudere la pagina o spegnere il PC.
2. Il pulsante «Fattura» su un singolo pagamento lo mette in coda **in testa**: parte entro pochi minuti. Dalla consegna 2b anche con l'intestatario scritto a mano («Altro»): va in coda, in testa, e i suoi dati si controllano all'accodamento (se sono incompleti la coda risponde 400 e non accoda niente).
3. Il menu «Coda fatture» mostra avanzamento, errori e orario stimato. Da lì: «Togli», «Rimetti in coda» e, solo per l'admin, «Sospendi».
4. Ritmo massimo: **50 fatture l'ora**, limite del Tier 0 del contratto Aruba meno il margine per il pannello. 300 fatture richiedono circa 6 ore.

## 2. Cose ancora aperte di R1 (da fare per prime)

- [x] **Orfane registrate: 6 su 7** il 23/09 alle 17:08–17:11, a coda sospesa, con ogni scrittura mostrata (audit 516–521). **Resta FPR 2524/2026 DA DECIDERE**: su Aruba ha un doppione valido emesso fuori dall'app il 23/09. Decide il commercialista (nota di variazione); poi, se va registrata, si usa `node scripts/fatture-orfane.mjs --applica --solo 11d88888-6d63-44ab-ab94-241efc05e937` a coda sospesa.
- [x] **Indagine chiusa: causa H1** (documenti emessi fuori dall'app con il numero scritto a mano). 7 salti su 7 spiegati, contatori corretti (nessun `--allinea`). Buchi: FPR 2155–2514 e Asilo 2530–2538. 25 doppioni: 11 storici regolari, 13 invii esterni scartati dallo SdI, 1 valido (FPR 2524). Prospetto per il commercialista in `$LAVORO/indagine-r1/prospetto-numerazione-2026.txt`. Dettaglio nel PRD, changelog del 23/09.
- [ ] **Da fare col titolare**: fermare le emissioni a mano dal pannello Aruba, oppure dare a chi le fa il numero giusto per serie. Portare al commercialista prospetto, buchi e FPR 2524.
  - Correggere i contatori (`--allinea`) **solo** se P6 è falso.
  - Il prospetto dei buchi va al commercialista.
- [x] PRD: «indagine in corso» sostituita con l'esito (PR-B #161; verificato il 23/09 sera: nel PRD la frase non c'è più).
- Controprove già fatte (23/09): `$LAVORO/indagine-r1/controprove.json`. `$LAVORO = /Users/lerri/kidville-lavoro/coda-fatture-aruba`, fuori dal repo, permessi 700.

## 3. Seconda consegna (dal piano completo)

Il piano completo è in `docs/superpowers/specs/2026-09-22-coda-fatture-aruba/`. Contiene `contratto.md` (unico, v4 + C0), i design `d1..d6`, `scomposizione.md` (49 fasi, 218 compiti) e `rilievi-residui.md`. Il nucleo è **un sottoinsieme semplificato**: prima di riprendere, riallineare contratto e scomposizione a ciò che il nucleo ha già costruito. Per esempio la tabella si chiama `fatture_coda`, non `fatture_coda_voci`, e non esistono `fatture_coda_invii` né il cancello condiviso.

### Rilievi emersi sul nucleo (PR #160, visti nella PR-B #161)

Sei punti verificati al 23/09. I primi cinque, (a)-(e), riguardano il nucleo così com'è in produzione oggi — nessuno blocca il rilascio già fatto. **(b), (c), (d) ed (e) sono chiusi dalla consegna 2a** (PR #162, in produzione dal 23/09 alle 23:07; piano in `consegna-2a-rilievi.md`). **(a) lo chiude la consegna 2b** (branch `fix/coda-fatture-rifiniture-2b`, piano in `consegna-2b-rifiniture.md`). Il sesto, (f), non è del nucleo: è un residuo che la fotografia rigenerata ha fatto vedere, e lo decide il titolare:

- **(a)** ✅ **Chiuso nella consegna 2b** (D1): anche l'intestatario scritto a mano va in coda. `zCorpoAccoda` accetta la persona in un gesto di **una** voce sola (il lotto resta al ramo `adult`), e la `POST /coda` la controlla con `validaCessionario`, le stesse regole dell'emissione, **prima** di ogni lettura: se è incompleta risponde 400 `INTESTATARIO_DIGITATO_INCOMPLETO` e non accoda niente. La persona resta in `fatture_coda.intestatario_scelto` finché la voce non è emessa o tolta; «Rimetti» la conserva, perché serve alla riemissione. Non esce dalla GET né dai log. «Ricorda sulla scheda» viaggia come `conferma_proposta` e lo scrive il lavoratore (`ricordaPersonaDigitata` → `ricordaPersonaSullaScheda`), solo dopo un'emissione nuova riuscita, con la riga del registro delle scritture che porta il valore sostituito, la sede e la classe del bambino. Eredita dalla PATCH il **perimetro di sede** (`assertAlunnoInScope`): scrive solo se il bambino sta in una delle sedi di chi ha accodato (`scuoleDiUtente`); se quelle sedi non si leggono non scrive niente; fuori sede non tocca la scheda né il registro, logga il `warn` `intestatario-persona-fuori-sede`, e la fattura resta emessa. ⏳ Limite dichiarato: dopo un trasferimento del bambino, sui pagamenti rimasti nella sede di partenza la casella non ha effetto e resta solo quel `warn`. Il rilievo com'era: **L'eccezione «Altro»**: con l'intestatario digitato a mano (`tipo: 'persona'`, non un adulto già in archivio) `FatturaButton.tsx` usa ancora la `POST /api/pagamenti/fattura` diretta, fuori dalla coda — è una decisione del direttore (documentata nel changelog del PRD), non una dimenticanza: `zCorpoAccoda` accetta solo il ramo `adult` perché la coda non custodisce dati digitati nel browser. Conseguenza: quel ramo scavalca il lavoratore, può contendersi il `signin` Aruba (1/minuto) con un giro della coda, e consuma lo stesso secchio orario senza che il lavoratore lo sappia in anticipo. Va portato in coda quando questa avrà una forma per l'intestatario "a mano".
- **(b)** ✅ **Chiuso nella consegna 2a**: la migrazione `20260923191725_fatture_coda_togli_azzera_esito.sql` rifà `fatture_coda_togli` identica a quella del nucleo più `esito_codice = NULL, esito_messaggio = NULL`, e un blocco `DO` ripulisce le voci già tolte (0 il 23/09); test su PGlite in `__tests__/db/fatture-coda-nucleo.test.ts`. L'ha applicata l'integrazione al merge della #162, ed è verificata con sole letture (§1). Il rilievo com'era: **`esito_messaggio` può contenere il nome di un genitore e resta dopo «Togli»**: la RPC `fatture_coda_togli` (righe 571-582 della migrazione `20260923102831_fatture_coda_nucleo.sql`) azzera `causale_manuale` e `intestatario_scelto`, ma **non** `esito_codice`/`esito_messaggio` — a differenza di `fatture_coda_rimetti`, che li azzera entrambi. Una voce tolta dopo un errore può quindi restare con un `esito_messaggio` che cita un nome in chiaro (messaggi d'errore Aruba/SDI). Da correggere: aggiungere `esito_codice = NULL, esito_messaggio = NULL` a `fatture_coda_togli`.
- **(c)** ✅ **Chiuso nella consegna 2a**: la funzione pura `src/lib/i18n/quando-relativo.ts` dice «oggi», «domani» o la data breve («ven 25/09»), sempre in Europe/Rome e sulle date civili; `codaFatture.stato.pausa` e `stimaFine` sono un `select` ICU sul giorno (it/en), e il pannello li usa per la pausa **e** per la fine stimata. Il rilievo com'era: **La stima di fine mostra solo l'ora**: `CodaFatturePanel.tsx` formatta `stima_fine` con `ora()` (solo HH:MM, via `useDateFormat`), senza data. Con 300+ fatture in coda (≈6 ore o più) la stima può cadere il giorno dopo, e «finisce alle 03:12» senza data è ambiguo o sbagliato.
- **(d)** ✅ **Chiuso nella consegna 2a**: tolte da `lotto-fatture.ts` le otto funzioni e costanti elencate qui sotto, più `RIFIUTI_LOCALI` e `CODICE_TRASPORTO_IGNOTO` che usavano solo loro (le copie vive stanno in `giro.ts`, `fattura/route.ts`, `esegui-blocco-fatture.ts`); tolti i loro casi di test (la copertura di `fermaIlLotto` su 504 e 429 è spostata, non persa), le 18 chiavi in `it` ed `en` e la voce `CONTATORI` del lock dei plurali (28 → 27). Nessun test protegge la rimozione: se una chiave o una funzione tornasse non diventerebbe rosso niente, la tengono i `grep` del piano. Il rilievo com'era: **Codice morto in `lotto-fatture.ts` e chiavi `reconLotto*` inutilizzate**: da quando `LottoFatturePanel.tsx` fa una sola `POST /coda` invece del vecchio ciclo a blocchi nel browser, sono rimaste senza chiamanti in `src/lib/pagamenti/lotto-fatture.ts`: `ATTESA_FRA_BLOCCHI_MS`, `DURATA_BLOCCO_STIMATA_MS`, `pausaDopo`, `PAUSA_DOPO_RIFIUTO_LOCALE_MS`, `pausaDopoBlocco`, `bloccoHaToccatoAruba`, `numeroInDubbio`, `stimaRimanenteMs`. In `messages/it/adminContabilita.json` restano 18 chiavi `reconLotto*` mai lette da nessun componente (`reconLottoAvanzamentoAttesa`, `reconLottoAvanzamentoInvio`, `reconLottoEmettiOra`, `reconLottoErroreEmissione`, `reconLottoFermatoGuasto`, `reconLottoFermatoSenzaNumero`, `reconLottoFermatoTrasporto`, `reconLottoGiaEmesse`, `reconLottoIgnote`, `reconLottoInCorsoTitolo`, `reconLottoInterrotto`, `reconLottoNonTentate`, `reconLottoNumero`, `reconLottoRiuscite`, `reconLottoSaltate`, `reconLottoStimaBreve`, `reconLottoStimaMinuti`, `reconLottoTetto`) — residuo della vecchia UI di avanzamento a blocchi. Le otto funzioni non hanno chiamanti nel codice di produzione, ma `__tests__/lib/lotto-fatture.test.ts` le importa: si tolgono **insieme** codice, i loro casi in quel test e chiavi i18n (`it` ed `en`), non spenti a metà — togliere solo il codice rompe il test, togliere solo il test lascia codice morto.
- **(e)** ✅ **Chiuso nella consegna 2a**: `GET /api/pagamenti` (solo staff) e `GET /api/pagamenti/riconciliazione` (solo righe confermate visibili) portano `coda_stato`, letto per sede e per stato da `leggiCodaAttiva` (`src/lib/fatture-coda/stato-righe.ts`), che non lancia mai e logga ogni degrado; chip «In coda» / «In invio» / «Errore in coda» in `FatturaChip` (tabella rette, tabella per categoria, card mobile) e in Riconciliazione (`CHIP_CODA`); in Alto Contrasto «Errore in coda» si ribalta a rosso pieno come «Scartata» (`kv-recon-chip--coda-errore` in `globals.css`). Il chip è la fotografia del caricamento, senza polling nuovo. Il rilievo com'era: **Badge «In coda» sulle righe**: la lista Pagamenti/Riconciliazione mostra lo stato di fatturazione (chip col numero, «Scartata, da riemettere», «In attesa SDI», «Da fatturare»), ma **non** un badge quando quella riga ha già una voce attiva in `fatture_coda` (in attesa o in invio). Oggi l'unico modo per saperlo è aprire la pagina «Coda fatture» a parte. Va aggiunto come badge sulla riga stessa.
- **(f) In produzione c'è ancora la tabella `backup_diario_vuote_20260908`** (residuo, decisione del titolare): le fotografie rigenerate nella PR-B #161 (`tabelle-scuola-id.json`, `pg-policies-snapshot.json`) la mostrano in `public`, senza `scuola_id` ma **legata all'alunno** (`alunno_id`, `maestra_id`, `tipo_evento`, `dettagli`, `nota_libera`, `nota_bambino`, …). È la copia delle **437 righe vuote del diario** cancellate l'08/09 (PRD, voce della pulizia del diario), fatta perché il `DELETE` prendesse gli id dalla copia. RLS attiva, nessuna policy: non è leggibile dai client, ma è un dato di minori tenuto oltre il suo scopo. **Toglierla è una scrittura in produzione e non spetta a PR-B**: il titolare decide se tenerla ancora (e fino a quando) o farla cadere con una migrazione dedicata, che aggiornerà di nuovo le due fotografie.

### Da fare, in ordine di valore

1. **Notifiche**: fine gruppo, errori, scarti SdI, pausa 429. Campanella più push, mai nomi; gli admin ricevono anomalie e «Da verificare» (decisioni 20–21).
2. **Verifica automatica degli esiti incerti** su Aruba: confronto su 5 campi, «Rimanda» con lo stesso XML e numero entro 12 giorni, 3×429, ricollegamento dei doppioni 00404 (decisioni 15, 16, 20). Oggi un esito incerto diventa `errore/esito_incerto` e si controlla a mano sul pannello.
3. **Cancello Aruba condiviso con la sync SdI**, più il giornale dei numeri (`fatture_coda_invii`) prima dell'upload.
4. **«Fattura tutto il periodo»** (periodo, base della data, categorie, sedi; massimo 500).
5. **Selezione multipla nella lista Pagamenti** e **sezione «Pagamenti e fatture» nella scheda alunno**.
6. Livelli di testa (rimandate → ritrasmissioni → urgenti), «Urgente» sulle selezioni multiple, causale scritta a mano modificabile dalla pagina.
7. 410 sulle vecchie route d'emissione; conservazione a 24 mesi con pulizia; test cardine su PGlite completo; E2E nuovi.
8. Note di credito TD04 (progetto separato, decisione 4).
9. Dei rilievi elencati sopra resta solo (f), che aspetta la decisione del titolare: (b)–(e) li ha chiusi la consegna 2a, (a) la 2b. Delle domande della 2a (`consegna-2a-rilievi.md` §7.2) la 2b ha deciso il «chi» della «Togli» (D2), la formula della stima (D3), il chip nel popup e nel dettaglio (D6) e «Invia fattura» sulle righe già in coda (D5); il codice del motivo su una voce tolta resta senza decisione, e azzerato. Restano al titolare le tre domande di `consegna-2b-rifiniture.md` §7.2 (la card «Da fatturare» del cruscotto, i due collegamenti su una riga in errore, la sola ricevuta per `gallery.published`): fino alla risposta valgono le scelte del piano.

Materiale utile già scritto e **non** integrato: `$LAVORO/r2a-f1-parziale-modifiche.patch` e `r2a-f1-parziale-nuovi.tgz`. È la fase R2A-F1 del piano completo (contratto-db.ts, Aruba finto, moduli), mai approvata dal critico.

## 4. Decisioni del titolare

In `/Users/lerri/.claude/plans/voglio-velocizzare-la-fatturazione-generic-marshmallow.md`: 29 decisioni definitive più i default. Le più importanti per chi riprende:
- ritmo fisso a 50/ora;
- data del documento = giorno d'invio;
- numero preso all'invio;
- ogni emissione passa dalla coda;
- tutta la segreteria vede tutta la coda;
- migrazioni solo dall'integrazione al merge;
- rilascio in autonomia;
- verifica dal vivo con fatture vere.

## 5. Metodo

Ogni fase è un Workflow: esecutori in parallelo su file disgiunti, poi un critico che verifica, rompe su una patch reversibile e fa il commit. Flussi riusabili:
- `esegui-fasi-coda-fatture` (legge la scomposizione);
- `nucleo-coda-fatture`.

Lezioni:
1. Il contratto unico va scritto **prima** dei progettisti in parallelo.
2. Il titolare chiede tempi brevi: consegne piccole e verificabili.
