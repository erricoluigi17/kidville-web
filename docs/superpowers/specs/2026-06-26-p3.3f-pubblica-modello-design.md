# P3.3f — Pubblica modello + link pubblico + config accessi + submission pubblica

**Fase:** P3 · **Slice:** P3.3f · **DL:** DL-030 · **Migrazione:** 20260747
**Branch:** feat/master-plan-registro

## Obiettivo
Permettere alla Segreteria di **pubblicare** un modello del Form Builder (`form_models`) e ottenere un
**link pubblico** condivisibile; consentire la **compilazione anonima** (senza login) del modello
pubblicato, con upload allegati token-scoped. Configurazione **accessi**: link pubblico (chiunque) vs
solo utenti registrati.

## Contesto (verificato live)
- `form_models` ha `is_active`, `is_enrollment_form`, `requires_signature` ma **nessuna** colonna
  publish/token. La compilazione interna è `/parent/forms/[id]` → `WizardContainer` → `/api/forms/submit`
  (DL-029) o `/api/forms/send-otp`.
- `/admin/modulistica` gestisce il sistema **legacy** `forms_templates` (≠ `form_models`); i `form_models`
  nascono dal builder `/admin/forms/builder` (POST `/api/admin/form-models`).
- Middleware: `PUBLIC_PREFIXES` in `src/lib/auth/middleware-rules.ts` (già include `/iscrizione`,
  `/api/forms`, `/forms`). Manca `/m` e `/api/public`.
- Upload pubblico precedente: `/api/iscrizione/upload` (service-role, anonimo).

## Decisioni di prodotto (default presi — coerenti con i pattern esistenti)
1. **Stato pubblicazione** su `form_models`: `published_at` (NULL = bozza), `public_token` (uuid unico,
   generato alla 1ª pubblicazione, **stabile** tra unpublish/republish), `access_mode`
   (`public` = chiunque col link · `authenticated` = solo registrati). Default `public`.
2. **Link pubblico:** `/m/{public_token}` (pagina top-level, fuori da `(dashboard)`).
3. **Submission pubblica = senza firma OTP** (`completed`). L'intake pubblico (iscrizioni/sondaggi) non
   richiede OTP; la **firma** pubblica (raccolta email firmatario) è materia della slice 3 (firma
   congiunta). Se un modello pubblicato ha `requires_signature`, il flusso pubblico raccoglie comunque
   le risposte come `completed` (nota in UI). I **consensi obbligatori** restano applicati (guard).
4. **Sicurezza:** endpoint pubblici **token-scoped** (validano `published_at IS NOT NULL` +
   `access_mode='public'`), service-role, **rate-limit**. Coerente con DL-029 (service-role + scoping app).

## Design

### Migrazione 20260747
```sql
ALTER TABLE form_models
  ADD COLUMN published_at timestamptz,
  ADD COLUMN public_token uuid UNIQUE,
  ADD COLUMN access_mode text NOT NULL DEFAULT 'public'
    CHECK (access_mode IN ('public','authenticated'));
```

### Middleware
`PUBLIC_PREFIXES += '/m', '/api/public'`. Test in `middleware-rules.test.ts`.

### Helper puro — `src/lib/forms/publish.ts`
- `publicFormUrl(token, origin?)` → `'/m/'+token` (path relativo). 
- `modelloPubblicato(model)` → boolean (`!!published_at`).
- `accessoConsentito(model, hasSession)` → boolean (public sempre; authenticated solo con sessione).
- (cuore TDD insieme a `consensi.ts` riusato).

### Endpoint admin — `POST /api/admin/form-models/publish` (gated `requireStaff`)
- body `{ id, action: 'publish'|'unpublish', access_mode? }`.
- publish: `public_token = coalesce(existing, gen_random_uuid())`, `published_at = now()`,
  `access_mode = body.access_mode ?? existing ?? 'public'`. Ritorna `{ public_token, url, access_mode }`.
- unpublish: `published_at = null` (token preservato per ripubblicazione con stesso link).

### Endpoint pubblici (token-scoped, service-role, rate-limit)
- `GET /api/public/forms/[token]` → modello pubblicato+public: `{ id, title, description, schema, requires_signature }`; 404 se non pubblicato; 403 se `access_mode='authenticated'`.
- `POST /api/public/forms/[token]/submit` → valida token/pubblicato/public; guard `consensiObbligatoriMancanti` → 400; inserisce `form_submissions` `completed` + `consents_log` (riusa helper DL-029); ritorna `{ id }`.
- `POST /api/public/forms/[token]/upload` → valida token; upload service-role in `form_attachments/public/{token}/…` (validazione tipo/dimensione come `/api/forms/upload`); ritorna `{ path }`.

### Pagina pubblica `/m/[token]/page.tsx`
- Server component: carica il modello via service-role per token. Se non pubblicato → `notFound()`.
  Se `access_mode='authenticated'` e nessuna sessione → schermata "accesso riservato agli iscritti".
- Rende `WizardContainer` in **modalità pubblica** (`publicToken`, `userId=null`,
  `requiresSignature=false`).

### `WizardContainer` — modalità pubblica
- Nuove prop opzionali: `publicToken?: string`. Quando presente: submit → `/api/public/forms/{token}/submit`,
  upload (via StepRenderer→FieldRenderer) → `/api/public/forms/{token}/upload`; forza path non-firma.

### Builder — controllo Pubblica
- `handleSave` cattura l'`id` del modello salvato. Mostra un pannello **"Pubblica / Copia link"**:
  toggle accesso (Pubblico/Registrati), bottone Pubblica→chiama publish, mostra il link `/m/{token}`
  con bottone Copia, bottone Annulla pubblicazione.

## Cosa NON è in scope
- Firma OTP su form pubblici (slice 3). Editing/loading di modelli esistenti nel builder (resta create-only;
  la pubblicazione opera sul modello appena salvato). Niente cron/solleciti.

## Testing (TDD)
- `publish.test.ts` (helper): url, modelloPubblicato, accessoConsentito.
- `middleware-rules.test.ts`: `/m/abc`, `/api/public/forms/x` pubblici; `/mx` non pubblico (no leak).
- `form-models-publish.test.ts`: gate; publish genera token+published_at+url; unpublish azzera published_at; republish riusa token.
- `public-forms-submit.test.ts`: 404 non pubblicato; 403 authenticated; 400 consenso obbligatorio mancante; 201 completed+consents_log.
- `public-forms-upload.test.ts`: 404 non pubblicato; 400 file/tipo; 200 path sotto public/{token}.

## DoD
TDD verde + lint + tsc + advisors 0 ERROR + PRD doppia scrittura (DL-030) + ROADMAP + memoria + commit.
